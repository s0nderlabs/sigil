// Tool: check_contract_code — eth_getCode RPC call
// Input: { address: string }
// Output: { isContract: boolean, codeSize: number }
export const checkContractCode = {}; // TODO: implement with Agent SDK tool()
