// Tool: get_token_balance — balanceOf() via eth_call (ERC-20/721/1155)
// Input: { address: string, tokenAddress: string, tokenId?: string }
// Output: { balance: string, decimals: number, symbol: string }
export const getTokenBalance = {}; // TODO: implement with Agent SDK tool()
