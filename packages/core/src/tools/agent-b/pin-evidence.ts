// Tool: pin_evidence — smart tool that auto-injects dataSnapshot from all previous tool calls
// Pins to Pinata IPFS, computes keccak256 hash on exact bytes pinned
// Input: { evidence: object } (Agent B provides reasoning, tool injects raw data)
// Output: { uri: string, hash: string }
export const pinEvidence = {}; // TODO: implement with Agent SDK tool()
