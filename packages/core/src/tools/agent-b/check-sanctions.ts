// Tool: check_sanctions — mock for hackathon (Chainalysis Oracle is mainnet only)
// Input: { address: string }
// Output: { sanctioned: boolean, source: string }
export const checkSanctions = {}; // TODO: implement with Agent SDK tool()
