// Tool: get_transaction_history — HyperSync query (pragma pattern)
// Input: { address: string, timeRange?: string, limit?: number }
// Output: { transactions: Array<{hash, from, to, value, gasUsed, timestamp, success}>, totalCount: number }
export const getTransactionHistory = {}; // TODO: implement with Agent SDK tool()
